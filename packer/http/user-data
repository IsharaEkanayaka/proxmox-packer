#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  network:
    network:
      version: 2
      ethernets:
        ens18:
          dhcp4: true
  ssh:
    install-server: true
    allow-pw: true
  identity:
    hostname: ubuntu-server
    username: ubuntu
    password: "$6$UrmZ4rka1FgTlY.o$Yds.bSh3L3GdhcvTS6kMmltO3.94pQMf/v.AojXwruQbGAfSkJp.FaOlzHg7evdIuho7M3Jq1RBzR89VnEsCi."
  storage:
    layout:
      name: direct
  late-commands:
    - echo 'ubuntu ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/ubuntu
    - curtin in-target --target=/target -- chmod 440 /etc/sudoers.d/ubuntu
  user-data:
    disable_root: false
    packages:
      - qemu-guest-agent
      - openssh-server
    package_update: true
    package_upgrade: false
    timezone: UTC
    runcmd:
      - systemctl enable ssh
      - systemctl start ssh
      - systemctl enable qemu-guest-agent
      - systemctl start qemu-guest-agent